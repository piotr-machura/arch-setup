#!/bin/bash

for arg in "$@"; do
    if [[ -f "$arg" ]] || [[ -d "$arg" ]]; then
        jupyter notebook "$arg" &>/dev/null &
        disown
    else
        echo "Not a file or directory: $arg"
    fi
done
